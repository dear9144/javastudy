package Ex01_field;

public class User {

	 /*
	  field 는 변수임
	  그러나 메소드가 가지는 변수가 아닐
	  class가 가지는 변수임을 암시 
	  */
	
	 String id; 
	 String pw;
	 int point; //사용자가 가지는 포인트가 잇다 치자
	 boolean isVip; //얘 vip니?
	 Address addr; //어드레스 타입 (클래스는 타입이 가능하기 때문)
	 //null 가짐 
	 
	 
	 
}
